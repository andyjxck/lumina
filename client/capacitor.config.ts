import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andysocial.dreamiestore',
  appName: 'dreamie.store',
  webDir: 'build'
};

export default config;
